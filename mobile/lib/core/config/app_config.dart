/// Firebase and API configuration constants.
class AppConfig {
  AppConfig._();

  /// Netlify Functions API base URL.
  static const String apiBaseUrl =
      'https://planula.netlify.app/.netlify/functions';

  /// Minimum Android SDK version supported.
  static const int minAndroidSdk = 24;
}
